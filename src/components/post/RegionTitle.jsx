import React from "react";
// src/components/post/RegionTitle.jsx

const RegionTitle = ({ region }) => {
  return <h2 className="text-xl font-semibold mb-2">{region}</h2>;
};

export default RegionTitle;
